# TASK-021 Adversarial Review Dispatch

## Review target

Perform one independent read-only review of the complete TASK-021 current shared bytes and evidence.

## Required challenges

1. Confirm Product Configuration `2.0.0` and QuoteLine `2.0.0` express the new breaking semantics without mutating any frozen `1.0.0` authority.
2. Confirm standard Track Length and Color are projected only from complete, published, uniquely resolvable real Article Number candidates; current FGD X15+PVC exposes only `6 m / Ivory White`, and no `4.3 m` or `7 m` is fabricated.
3. Confirm zero/multiple candidate, conflicting stable identity, duplicate Article Number, duplicate product choice and invalid/unpublished candidate paths fail closed without guessing.
4. Confirm Installation is absent from the customer configuration, QuoteLine v2, validation errors and summary while the product fact that ceiling/wall mounting is supported remains outside customer option identity.
5. Confirm Custom Length remains a sibling of standard length, accepts only positive exact one-decimal-safe values, produces `sales_follow_up`, and never fabricates an Article Number.
6. Confirm Packaging, Logo Printing, protection arrangement, positive whole-number quantity and one-latest in-memory replacement remain correct and customer-readable.
7. Confirm the browser-facing Client Component and Next/Flight bytes contain no Article Number, product UUID, raw internal DTO/enum, WordPress/Feishu identifier, secret, diagnostic or internal business field.
8. Confirm the CMS loader uses exactly one `/resolve` and one `/product-configurations` request, no ProductCard or per-option request, and no browser request to WordPress or Feishu.
9. Confirm preview/cms remain local non-production and `noindex,nofollow`, production remains 404, and no Basket, persistence, submission, Feishu, related products or deployment behavior was smuggled into scope.
10. Reproduce the focused/full test, five verifier, lint, typecheck, build/smoke, protected authority, visual evidence encoding/hash, cleanup, diff and DPG boundaries proportionately.

## Preserved history

- Visual Round 1: `FAIL / severe=1 / obvious=1 / detail=1`.
- Frontend Visual R1 narrow revision checkpoint: PASS.
- Visual Round 2: `PASS / severe=0 / obvious=0 / detail=0`.
- Planner pre-review validation: PASS.

Do not overwrite or collapse any earlier failure or recovery history.

## Output and authority

Return one controlled review response with `PASS` or `FAIL` and exact `P0/P1/P2` counts. Write only the canonical TASK-021 adversarial review report, adversarial_reviewer lane records and controlled response. Do not repair product code, CMS, task authority, README, tests or visual evidence. Do not accept the task, commit, push, merge, deploy or start deferred features.
