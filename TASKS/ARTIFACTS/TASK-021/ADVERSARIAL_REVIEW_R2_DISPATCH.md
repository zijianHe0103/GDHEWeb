# TASK-021 Adversarial Review Round 2 Dispatch

## Authority

- Historical Round 1: `FAIL / P0=0 / P1=2 / P2=1`.
- User decision A: visible Add to Quote creates one browser-memory PublicQuoteDraft; QuoteLine v2 is only a future server-side conversion contract.
- Review only the three Round 1 findings against current bytes. Do not reopen passing unrelated scope unless the narrow revision created a direct regression.

## Required Independent Review

### P1-1

- reproduce real complete-root `4.3`, `5.8`, `6.7` positive and `6.05` negative through both CMS/Python evidence and frontend production Ajv;
- confirm exact decimal handling does not change Product Configuration v2 Schema/Golden/error/runtime/API/PHP/Fixture business bytes;
- confirm frontend Schema bytes remain exact source copies and no permissive validation widening beyond the one-tenth contract occurred.

### P1-2

- confirm requirements/design/acceptance/docs and production symbols consistently describe PublicQuoteDraft, not a completed QuoteLine;
- confirm production uses only `latestDraft` / `LatestPublicQuoteDraftSummary`, while old line names remain at most negative tests;
- confirm QuoteLine v2 has no production caller and no conversion route is fabricated;
- confirm real preview HTML/Flight contains no Article Number, stable UUID, internal field, raw enum, `sales_follow_up`, WordPress/Feishu marker, secret or diagnostic;
- confirm one-latest, refresh-clears, zero network/storage/submission and unchanged visible copy/layout.

### P2-1

- independently verify final CMS manifest hash `11f3db81c1b962c387f731d9c171d2f370ba60bdc3391cc10ec991247120ac09` and checksum-stream hash `fe611983112944edcf214d88a9aefac6cc4fa4b9258f07670870414a919204ca`;
- independently expand/verify literal `20/20` and confirm frontend pins match only this final authority;
- preserve the intermediate and Round 1 stale-hash history without treating it as current authority.

## Regression And Governance Gates

- reproduce focused/full tests, five verifiers, lint, typecheck, build and production smokes proportionate to the findings;
- verify Visual Round 1 `FAIL / 1 / 1 / 1`, frontend revision PASS and Visual Round 2 `PASS / 0 / 0 / 0` histories plus 23/23 evidence remain unchanged;
- verify v1/package/lock/protected image/CSS/next-env/protected scope and zero generated/listener residue;
- report `PASS`, `FAIL` or `BLOCKED` with exact P0/P1/P2 counts.

## Reviewer Boundary

Read-only business review. The reviewer may write only the canonical review report, reviewer worklog and controlled response messages. Do not repair frontend/CMS/product/Planner authority, mutate database, run external systems, commit, push, merge or deploy. PASS permits only fresh Planner final validation after response acknowledgement; it is not user acceptance or Git authorization.
