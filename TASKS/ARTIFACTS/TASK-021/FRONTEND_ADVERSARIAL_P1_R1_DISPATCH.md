# TASK-021 Frontend Adversarial P1 Round 1 Dispatch

## Authority

- Adversarial Round 1 remains `FAIL / P0=0 / P1=2 / P2=1`.
- User selected A: the visible `Add to Quote` action creates and replaces one browser-memory `PublicQuoteDraft`; it does not create QuoteLine 2.0.0.
- QuoteLine 2.0.0 remains an isolated future server-side conversion contract for the final Request a Quote flow.
- Begin only after the WordPress exact-decimal revision has produced and Planner has validated the final current literal `20/20` handoff.

## Required RED/GREEN

### P1-1: Product Configuration v2 exact one-tenth validation

1. Preserve a direct RED through the production Product Configuration v2 runtime registry showing the current Ajv float behavior does not accept every legitimate full-root document using `4.3`, `5.8` and `6.7`.
2. Add the minimum explicit Ajv `multipleOfPrecision` handling at the Product Configuration v2 registry boundary, consistent with the repository's existing closed-contract convention. Do not change frozen Schema bytes.
3. Prove full-root `4.3`, `5.8` and `6.7` positive documents pass and `6.05` fails. Preserve all prior Schema, semantic, authentic-wrapper and normalized-error gates.
4. Refresh the frontend Product Configuration v2 authority pins and local snapshot manifest/checksum evidence against the Planner-validated final CMS handoff. Exact Schema/Golden/error snapshot bytes must remain source-identical unless the final CMS authority proves otherwise; no manual hash guessing.

### P1-2: truthful public-draft production semantics

1. Preserve a direct RED proving production symbols/tests still call the browser result `latestLine` / `LatestQuoteLineSummary` or otherwise claim that Add to Quote creates QuoteLine 2.0.0.
2. Rename only the production/public presentation seam to truthful `PublicQuoteDraft` semantics, including the result-state property, summary component/export and direct tests. Prefer `latestDraft` and `LatestPublicQuoteDraftSummary` unless an equally explicit existing style is required.
3. Preserve visible customer copy, field order, CSS/layout, one-latest replacement, refresh-clears behavior, zero network/storage/submission and all browser-byte internal-identity exclusions.
4. Keep the isolated QuoteLine 2.0.0 Schema/samples/verifier/builder as a future server-owned conversion contract with no production caller. Do not fabricate a server route or claim the future conversion exists.
5. Reprove rendered markup and Next/Flight contain no Article Number, stable internal UUID, WordPress/Feishu ID, raw enum, `sales_follow_up`, secret or diagnostic.

## Required Validation

- focused exact-decimal full-root and public-draft RED/GREEN tests;
- Product Configuration v2 snapshot/verifier/runtime/consumer/server-only gates;
- Product Configurator interaction/presentation/summary/real-preview response gates;
- all five contract verifiers, lint, typecheck, production build and existing production smokes;
- v1/package/lock/protected-image/visual-source/protected-scope preservation;
- zero listener/temp/generated residue, diff and DPG gates.

## Allowed Writes

- minimum Product Configuration v2 frontend registry/tests and authority pin/snapshot manifest files required by the final CMS handoff;
- minimum Product Configurator public state/component/tests required for truthful `PublicQuoteDraft` naming;
- `frontend/README.md` and `docs/frontend/PRODUCT_CONFIGURATION_AND_QUOTE_LINE_CONTRACT.md` only if current symbol/authority wording requires exact synchronization;
- existing TASK-021 frontend execution/validation/diff/TDD evidence, `LANES/frontend/worklog.md`, and controlled messages/events.

## Forbidden Scope

Do not change WordPress/CMS/database, frozen Product Configuration v1 or QuoteLine v1/v2 authority bytes, Product Detail facts, ProductCard/ProductList, visible layout/CSS/customer copy, protected image, package/lock/dependencies, Basket, 30-day persistence, submission/API, server session, Feishu, related products, visual evidence bytes, Planner authority, review, acceptance, Git or deployment.

## Return

Return one linked execution response with both real REDs, focused/full counts, final CMS authority hashes/pin parity, changed-file inventory, browser-byte/security proof and protected-boundary results. This is a lane checkpoint, not adversarial Round 2 PASS, user acceptance or Git authorization.
